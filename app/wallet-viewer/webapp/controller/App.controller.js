sap.ui.define([
    "sap/ui/core/mvc/Controller"
], function (Controller) {
    "use strict";

    /**
     * App Controller
     * @namespace odatanoview.walletviewer.controller
     */
    return Controller.extend("odatanoview.walletviewer.controller.App", {

        onInit: function () {
            // Simple app init controller - routing is handled by manifest.json
        }
    });
});
