import Controller from "sap/ui/core/mvc/Controller";

/**
 * App Controller
 * @namespace odatanoview.walletviewer.controller
 */
export default class App extends Controller {

    public onInit(): void {
        // Simple app init controller - routing is handled by manifest.json
    }
}
